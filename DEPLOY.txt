Ghost Sentinel Hub v4.1
- Auto-rejoin lobby on refresh/reconnect (remembers username)
- Ping indicator (RTT) every 5s

Render start command:
gunicorn -k geventwebsocket.gunicorn.workers.GeventWebSocketWorker -w 1 ghost_hub:app


v4.2.1 hotfix: fix bot help SyntaxError (_bot_emit line)

Hotfix v4.2.2: Python boolean tokens fixed (false/true/null).

Hotfix v4.2.3: removed stray escape characters in bot emit calls.

Update v4.3: expanded world/home designer help+examples, `!help world`, `!help home`, and a narrative beat after key commands.

Update v4.4: Choose-Your-Own-Adventure engine with branching nodes, `!adv`, `!choose`, `!choices`, and on-page choice buttons.

Update v4.5: Adventure upgrades (locked choices + inventory + random encounters) and a tiny live world-map panel. Help + Quick Starts updated.
